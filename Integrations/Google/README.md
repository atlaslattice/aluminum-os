# Google Integrations (Universal Interop Layer)

Part of the 12×12×12 Riemann rainbow lattice build system.

## Purpose
- Grounded Google API surface via the MCP server (standard OAuth + google-api-python-client, no geometry in code).
- Cross-platform bridges between Microsoft deep substrate (OneDrive primary build + A2A watchers + Graph + Purview governance) and Google (Drive/Docs parallel canon candidates, Gmail/Calendar comms, Maps geo, YouTube media/harvest).
- All activity tagged with H/S/A lattice_coords + epistemic labels. Additive only (INV-17).

## Files
- bridge_sync.py : `python bridge_sync.py --spec "Moon_Party_Harvest_Build_Spec_v2.0.md" --coords H02-S01-A09`
  Mirrors a canon spec to a Google Doc (create_google_doc). Emits A2A result JSON.
- (future) realtime_bridge.py , presence_federation.py , search_unify.py

## CLI
From anywhere (after lattice installed):
lattice google doc create "My Canon Mirror Test" "Content from OneDrive spec..."
lattice google drive list "name contains 'Lattice'"
lattice canon google-doc-create "Title" "Body"   # alias

## Mappings (see full in Schemas/Build_System_Schema_Registry.xlsx + TermStore)
49-72 cover Google MCP + bridges (Drive H02-S01G-A03, Docs H01-S01G-A02, ... meta H12-S12-A06).

## Promotion / Canon Rules (still apply)
- Local/OneDrive = candidate.
- Google Docs created here are *parallel mirrors* for resilience / cross-cloud access.
- Only website-published content is canon. Google mirror does not bypass human-gate + re-ingest.
- Use bridge from promotion queue or A2A google_mirror_canon type.

## Setup
See Google_MCP_Server/README.md for client_secrets + pip.

MUTANT AND PROUD. Universal interop across platforms (MS + Google; Apple surrendered).
The 12x12x12 classifies it all.